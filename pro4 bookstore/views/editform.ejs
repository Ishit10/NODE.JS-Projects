<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Book</title>
    <style>
        body {
            background-color: #2c3e50;
            color: white;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: #34495e;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 300px;
        }
        h2 {
            text-align: center;
            color: #e74c3c;
        }
        label {
            display: block;
            margin-top: 10px;
        }
        input {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            border-radius: 5px;
            border: none;
        }
        button {
            width: 100%;
            padding: 10px;
            margin-top: 15px;
            background-color: #e74c3c;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #c0392b;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Update Book</h2>
        <form action="/update/<%= userdata.id %>" method="post" enctype="multipart/form-data">
            <label for="">Name</label>
            <input type="text" name="userName" value="<%= userdata.userName %>" required />
            <label for="">Email</label>
            <input type="email" name="email" value="<%= userdata.email %>" required />
            <label for="">Author</label>
            <input type="text" name="author" value="<%= userdata.author %>" required />
            <label for="">Price</label>
            <input type="text" name="price" value="<%= userdata.price %>" required />
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>